package Task;

import java.time.LocalDate;

public class MonthlyTask extends Task {
    @Override
    public boolean appearsIn(LocalDate localDate) {
        return super.appearsIn(localDate);
    }
}
